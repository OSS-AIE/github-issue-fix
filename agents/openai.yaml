interface:
  display_name: "GitHub Issue Fix"
  short_description: "Fix upstream GitHub issues and PR CI with quality gates"
  default_prompt: "Use github-issue-fix. Issue or PR: <link>. Triage, fix, validate, and prepare or update the PR."
